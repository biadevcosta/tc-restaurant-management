spring:
  datasource:
    url: jdbc:mysql://localhost:3306/restaurant_db?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true
    username: ${DB_USER:admin}
    password: ${DB_PASS:secret123}
    driver-class-name: com.mysql.cj.jdbc.Driver
